\define export-to-html(filter, fileName)
<$button>
<$action-sendmessage $message="tm-download-file"
$param="$:/core/templates/exporters/StaticRiver"
exportFilter="""$filter$""" filename="""$fileName$"""/>
{{$:/core/images/export-button}} export ''<$count filter="$filter$"/>''
tiddler(s) in filter:<br/>
<br/>
$filter$<br/>
<br/>
Save to file:

$fileName$
</$button>
\end

<<export-to-html
"[all[tiddlers]tag[INDEX]sort[title]][all[tiddlers]!tag[EXTLINK]!tag[INDEX]!tag[META]!prefix[Draft of]!is[image]!is[tag]!is[system]sort[title]]"
>>


Plan:

Create this macro
Create new tiddler shortcut for alt-h
Point the alt-h command to this macro

Change Tiddler_Move to read the first 100 bytes of the file
If the text "MOTW Comment" is there, use FileIO_TW_HrefRename.wHtml_Code_Text to create/overwrite the ".html" file next to the found ".htm"

The !is[tag] filter needs removed when a tag has content


$:/config/ShortcutInfo/save-html-converted

keyboard action SaveHtmlConverted

 $:/tags/KeyboardShortcut

<$action-sendmessage $message="tm-download-file"
$param="$:/core/templates/exporters/StaticRiver"
exportFilter="""[all[tiddlers]tag[INDEX]sort[title]][all[tiddlers]!tag[EXTLINK]!tag[INDEX]!tag[META]!prefix[Draft of]!is[image]!is[tag]!is[system]sort[title]]"""/>

key: ((save-html-converted))